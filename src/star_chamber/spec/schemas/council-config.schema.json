{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/peteski22/agent-pragma/spec/council-protocol/schemas/council-config.schema.json",
  "title": "Council Configuration",
  "description": "Full configuration file for the council protocol (providers array + settings).",
  "type": "object",
  "required": ["providers"],
  "properties": {
    "providers": {
      "type": "array",
      "description": "List of LLM providers to consult.",
      "items": {
        "$ref": "provider-config.schema.json"
      },
      "minItems": 1
    },
    "otari": {
      "type": "object",
      "description": "Route all non-local providers through Otari, an OpenAI-compatible LLM gateway. When set, each provider call is dispatched through the `otari` provider with the shared api_base/api_key; the per-provider `provider` field is treated as a label.",
      "properties": {
        "api_base": {
          "type": "string",
          "description": "Otari base URL or ${ENV_VAR} reference. Falls back to the OTARI_API_BASE environment variable when omitted."
        },
        "api_key": {
          "type": "string",
          "description": "Otari API key or ${ENV_VAR} reference. Falls back to the OTARI_API_KEY environment variable when omitted."
        }
      },
      "additionalProperties": false
    },
    "timeout_seconds": {
      "type": "integer",
      "description": "Default per-provider timeout in seconds.",
      "default": 60,
      "minimum": 1
    },
    "consensus_threshold": {
      "type": "integer",
      "description": "Minimum number of providers that must agree for consensus.",
      "default": 2,
      "minimum": 1
    }
  },
  "additionalProperties": false
}
